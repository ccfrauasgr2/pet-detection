apiVersion: v1
kind: Service
metadata:
  name: restapi-svc
spec:
  selector:
    app: restapi
  ports:
    - protocol: TCP
      port: 8080 #TODO
      targetPort: 8000 #TODO
  type: LoadBalancer
---  
apiVersion: apps/v1
kind: Deployment
metadata:
  name: restapi-deployment
spec:
  selector:
    matchLabels:
      app: restapi 
  replicas: 3
  template:
    metadata:
      labels:
        app: restapi
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - restapi
            topologyKey: kubernetes.io/hostname
      containers:
      - name: restapi
        image: skywalker360/pet-detection-repo:1.1 #TODO
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000 #TODO
        env:
        - name: MONGO_INITDB_ROOT_USERNAME
          valueFrom:
            secretKeyRef:
              name: mongo-secret
              key: mongo-user
        - name: MONGO_INITDB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mongo-secret
              key: mongo-password 
        - name: MONGO_INITDB_CONFIG #TODO
          valueFrom:
            configMapKeyRef:
              name: mongo-config
              key: mongo-read-url