 apiVersion: openebs.io/v1alpha1
 kind: JivaVolumePolicy
 metadata:
   name: default-jivavolumepolicy
   namespace: openebs
 spec:
   replicaSC: openebs-hostpath
   target:
     replicationFactor: 3
     affinity:
       podAffinity:
         requiredDuringSchedulingIgnoredDuringExecution:
         - labelSelector:
             matchExpressions:
             - key: openebs.io/target-affinity
               operator: In
               values:
               - mysql-sts  # for policy to work, "openebs.io/target-affinity: mysql-sts" must be specified in mysql StatefulSet manifest file, under spec.template.metadata.labels                      
           topologyKey: kubernetes.io/hostname
           namespaces: ["default"]                     
   replica:
    affinity:
      podAntiAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
        - labelSelector:
            matchLabels:
              openebs.io/replica-anti-affinity: mysql-sts # for policy to work, "openebs.io/replica-anti-affinity: mysql-sts" must be specified in mysql StatefulSet manifest file, under spec.template.metadata.labels                      
          topologyKey: kubernetes.io/hostname